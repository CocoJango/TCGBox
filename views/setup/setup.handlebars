<h1 class="uk-heading-line uk-text-center"><span>Welcome to TCGBox</span></h1>
<p class="uk-text-lead uk-text-center">It seems this is a new installation of TCGBox. Let's start by creating a user
    account for you.</p>

<div class="uk-card uk-card-default uk-card-body uk-card-large">
    <p>
    <form action="/users" method="post" class="uk-form-horizontal" enctype="application/x-www-form-urlencoded">
        <div class="uk-margin">
            <label for="name" class="uk-form-label">Name <br>
                <span class="uk-label uk-label-danger">{{#with (fieldInError errors "name")}}{{this.msg}}{{/with}}</span></label>
            <input type="text" class="uk-input {{#if (fieldInError errors "name")}}uk-form-danger{{/if}}" name="name" id="name" value="{{user.name}}">
        </div>
        <div class="uk-margin">
            <label for="email" class="uk-form-label">E-Mail <br>
                <span class="uk-label uk-label-danger">{{#with (fieldInError errors "email")}}{{this.msg}}{{/with}}</span></label>
            <input type="text" class="uk-input {{#if (fieldInError errors "email")}}uk-form-danger{{/if}}" name="email" id="email" value="{{user.email}}">
        </div>
        <div class="uk-margin">
            <label for="password" class="uk-form-label">Password <br>
                <span class="uk-label uk-label-danger">{{#with (fieldInError errors "password")}}{{this.msg}}{{/with}}</span></label>
            <input type="password" class="uk-input {{#if (fieldInError errors "password")}}uk-form-danger{{/if}}" name="password" id="password">
        </div>
        <div class="uk-margin">
            <input type="submit" class="uk-button uk-button-primary uk-width-1-1">
        </div>

    </form>
    </p>
</div>
